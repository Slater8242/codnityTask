import { Box, Typography } from '@mui/material';

const PageNotFound = () => (
  <Box sx={{
    height: '100%',
    display: 'flex',
  }}
  >
    <Typography variant="h1">
      Page not found
    </Typography>
  </Box>
);

export default PageNotFound;
