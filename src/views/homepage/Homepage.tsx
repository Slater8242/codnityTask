import Layout from '../../components/layout/Layout';

const Homepage = () => {
  const str = 'test';

  return (
    <Layout>
      {str}
      {str}
      {str}
      {str}
    </Layout>
  );
};

export default Homepage;
